<div class="card-header">製品情報詳細</div>
<div class="card-body">
    <ul class="list-group list-group-flush">
        <li class="list-group-item">
            <h5>大カテゴリ名</h5>
            <span><%= product.first_category.name %></span>
        </li>
        <li class="list-group-item">
            <h5>中カテゴリ名</h5>
            <span><%= product.second_category.name %></span>
        </li>
        <li class="list-group-item">
            <h5>タグ名</h5>
            <% product.tags.each do |tag| %>
                <span><%= tag.name %></span>  
            <% end %>
        </li>
        <li class="list-group-item">
            <h5>製品名</h5>
            <span><%= product.name %></span>
        </li>
        <li class="list-group-item">
            <h5>製品画像</h5>
            <%= image_tag product.image, class: "w-100" %>
        </li>
        <li class="list-group-item">
            <h5>製品詳細</h5>
            <span><%= simple_format(h(product.detail)) %></span>
        </li>
        <li class="list-group-item">
            <h5>表示設定</h5>
            <span><%= ProductConstants::RELEASE_FLG_LIST[product.release_flg] %></span>
        </li>
    </ul>
</div>
<div class="card-footer">
    <div class="row">
        <div class="col-12 text-center my-2">
            <%= link_to "編集", edit_admin_product_path(product), class: "btn btn-primary w-50", role: "button" %>
            <%= link_to "戻る", admin_products_path, class: "btn btn-secondary", role: "button" %>
            <!-- Button trigger modal -->
            <button type="button" class="btn btn-outline-danger float-end" data-bs-toggle="modal" data-bs-target="#destroyModal">削除</button>
        </div>
    </div>
</div>
